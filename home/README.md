# Home tab

- **Public URL:** `/` → root `index.html`
- **What lives here:** Landing hero, profile summary, skills, timeline (experience), featured projects, and a short link to Data Viz.
- **Related assets:** `css/site.css`, `css/about.css` (timeline), `js/site-config.js`, `js/site-ui.js`, `timeline-data.js` (repo root — shared with the former standalone About page, now merged into Home).

Edit copy in `js/site-config.js` or directly in `index.html`. Timeline data: `timeline-data.js`.
